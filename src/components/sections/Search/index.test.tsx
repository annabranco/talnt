import { shallow, ShallowWrapper } from 'enzyme';

describe('< Article >', () => {
  xit('should mount', () => {});
  xit('Should display a search input with a placeholder "search (min. 3 char)" and no value', () => {});
  xit('should display a minimized placeholder when any text is written on the input', () => {});
  xit('Should display a disabled search button with the text "search"', () => {});
  xit('should enable the search button when any text in written in the input', () => {});
  xit('should make an api call using the text written on the input as a query param', () => {});
  xit('should trigger a onSearch function received by props when data is received from server', () => {});
  xit('should not break if ther api call fails', () => {});
});
